"""UV Studio direct MCP discovery package.

Keep package initialization intentionally light: capability adapters import
`uv_studio.mcp.models`, so eager re-exports here would create an import cycle.
"""
