# Next Task

<!-- uv-next-slice: product-recovery-story-orchestration -->

## Goal

Recover the preparation-only `story_video` journey through the permanent Product Orchestrator after General Video recovery is complete. Reuse its existing Stage 8 story workspace, Project Store media identity, current continuity/planning/editor primitives and Capability Registry boundaries; do not create a parallel story workflow database or restore retired pipeline routes.

## Required direction

- inventory the as-built story brief/script, ordered story media, continuity/planning state, generation inputs, assembly/render state and current user-visible surface before changing orchestration;
- project truthful readiness, prerequisites, relevant workspaces, diagnostics, current outcome and bounded semantic actions through Product Orchestrator;
- keep Project Store and existing accepted domain/editor state canonical; Product Orchestrator remains a current-state projection plus semantic-action boundary;
- reuse D-033 editor primitives and existing deterministic media adapters before adding story-specific editor mechanics;
- route generation/provider/runtime work through Capability Registry and D-017 boundaries and never claim provider-backed generation is local or free when it is not;
- verify project-owned bytes wherever current media identity or output truth matters and fail closed on stale/tampered state;
- preserve Photo, Visualizer, Targeted Edit, Dubbing, Music, Narrated and General Video journeys plus strict Project Store behavior;
- keep retired legacy frontend/backend surfaces retired;
- add focused orchestration/API/browser proof and preserve all five permanent CI checks.

## Completion proof

The slice is complete when `story_video` has a truthful authoritative Product Orchestrator journey over existing UV-owned state, can reach the furthest currently supported project-owned result through bounded semantic actions without hidden state seeding, and exact Draft and Review heads pass all five permanent Ubuntu/Windows CI jobs.

## Entry gate

Begin only from idle `main` after General Video PR #53 is reviewed, merged and lifecycle closure records the General Video journey as authoritative. Commercial Product remains a separate follow-up recovery path rather than being silently bundled into Story.
