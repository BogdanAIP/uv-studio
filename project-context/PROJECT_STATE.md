# Project State

<!-- uv-context-state: draft -->
<!-- uv-active-slice: project-identity-v2-compat-reader -->

**Updated:** 2026-09-02

**Repository:** `BogdanAIP/uv-studio`

## Current lifecycle

`project-identity-v2-compat-reader` is back in `draft` for PR #89 on branch `stage-19/project-identity-v2-compat-reader`, based on lifecycle-closed `main` at `52be1939eca51d7147990288cfc6258b023c2cd2`.

A fresh ordinary-ChatGPT review of frozen head `b965d1dfcc1426226da501c57835d1508886b138` returned one `CURRENT` P1. Redo-owned managed-media authority is currently reduced to path-only state. When a successful Generation artifact and its Take are both undone, the generated bytes remain on disk for Redo, but archive export and startup preservation no longer retain the historical redo-only ProjectReference that carries Generation Job/Attempt provenance plus persisted `size_bytes` and SHA-256. Corrupted or replaced bytes at the same path can therefore survive archive/restart preservation and later Redo can restore the historical reference/Take around the wrong binary payload.

The live P1 thread is `PRRT_kwDOT0Lyms6edmby` at `uv_studio/projects/archive.py:244`. PR #89 was returned to Draft before runtime/test repair.

## Ninth repair in progress

The repair must preserve full validated redo reference authority rather than only path strings. Redo-only Generation ProjectReferences must be reconstructed from committed `history.entries[history.cursor:]` Project snapshots and checked against the same durable Generation Job/Attempt, contract, path, `size_bytes` and SHA-256 authority used for live Generation artifacts before archive capture or startup preservation. Malformed, duplicate or conflicting redo authority must fail closed. Non-Generation redo-owned media remains protected by validated redo reachability as before.

Regression coverage must include a real successful Generation publication, two Undo operations removing the Take and artifact while preserving bytes, deliberate byte corruption while the artifact is redo-only, and rejection by both archive export and restart recovery/preservation. The unchanged-byte two Undo -> export/import -> two Redo identity/byte preservation contract must continue to pass, as must redo truncation and the prepared-UOW recovery repair.

## Accepted evidence before this repair

The eighth repair material/test head `80fe2b3076b64a94c8d6ebc7118f79c3af88688d` passed CI #4418 (`33601095299`) **5/5**. Its final synchronized Draft head `1f36c87a2f4cd1b0c9c2b4376ede7089b3f991c4` passed CI #4422 (`33601720381`) **5/5**. The context-only frozen review head `b965d1dfcc1426226da501c57835d1508886b138` then passed authoritative post-Ready CI #4428 (`33618992605`) **5/5** before the fresh P1 finding was reported.

All earlier Stage-19 repairs remain intended to stay in force: schema-v1/v2 compatibility and exact historical identity, prepared-UOW recovery before archive sampling, archive locking/snapshot authority, staged/fenced publication, Generation Job/artifact/Take recovery and live digest checks, explicit Take Undo preservation, source/WebVTT/legacy-art/prepared-audio recovery, redo-owned media preservation, publication-marker reference identity, and immediate-next-action Product Truth behavior.

## Next required action

1. add deterministic redo-only Generation corruption regressions for archive and restart paths;
2. repair archive/recovery redo authority without creating a second binary-history authority;
3. run exact material-head CI 5/5;
4. synchronize docs/context/PR body and run final Draft exact-head CI 5/5;
5. reply to and resolve the P1 thread only after evidence is green;
6. refreeze context-only to `review`, mark Ready, require post-Ready exact-head CI 5/5 and run another completely fresh ordinary-ChatGPT semantic review;
7. merge only on `CURRENT PASS` with zero findings and clean final live identity.

After merge, D-038 lifecycle closure to `idle` remains mandatory before starting the declared handoff.

## Out of scope

Recipe endpoint retirement, execution-plan retirement, Product Orchestrator redesign/retirement, Stage8 retirement, provider-selection redesign, Production Direction authority changes, Timeline identity redesign and later D-070 compression work remain separate slices.
