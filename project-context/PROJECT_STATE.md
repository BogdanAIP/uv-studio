# Project State

<!-- uv-context-state: draft -->
<!-- uv-active-slice: project-identity-v2-compat-reader -->

**Updated:** 2026-09-02

**Repository:** `BogdanAIP/uv-studio`

## Current lifecycle

`project-identity-v2-compat-reader` remains in `draft` for PR #89 on branch `stage-19/project-identity-v2-compat-reader`, based on lifecycle-closed `main` at `52be1939eca51d7147990288cfc6258b023c2cd2`.

A fresh ordinary-ChatGPT review of frozen head `b965d1dfcc1426226da501c57835d1508886b138` returned one `CURRENT` P1. Redo-owned managed-media authority had been reduced to path-only state. When a successful Generation artifact and its Take were both undone, generated bytes remained on disk for Redo while archive export/startup preservation no longer retained the historical redo-only ProjectReference carrying Generation Job/Attempt provenance plus persisted `size_bytes` and SHA-256. Corrupted or replaced bytes at the same path could therefore survive archive/restart preservation and later Redo could restore historical artifact/Take JSON around the wrong binary payload.

The live P1 thread is `PRRT_kwDOT0Lyms6edmby` at `uv_studio/projects/archive.py:244`. PR #89 returned to Draft before the runtime/test repair.

## Ninth repair implemented

Regression-first commit `d5b4a7cbcc85e7895d68263cf80bd0f4d7512a9e` adds a real GenerationService harness rather than generic metadata. It proves a successful Generation publication, two Undo operations removing Take and artifact while retaining bytes, same-size byte corruption, and fail-closed behavior for archive export, startup project recovery and direct `ProjectUnitOfWork.redo()`. The same harness proves unchanged exact Generation bytes survive archive/import and two Redos with the original artifact metadata and Take identity.

Shared authority commit `4645de0b91fad5519f8ef5e47eedbd29767553b6` adds `uv_studio/projects/generation_authority.py`. One structural boundary now validates a Generation ProjectReference against its safe durable Job record, exact historical Attempt, model/capability/offer/adapter mapping, request digest, generation contract, attempt-derived output path, historical Take identity and persisted positive `size_bytes`/SHA-256; a byte validator additionally verifies the exact current file size/digest.

`c61d36a9e81061f2724ecdb2bea4a334889a132e` extends ProjectUnitOfWork with validated `redo_project_documents()`, which reconstructs current redo-suffix Project snapshots from committed transaction records/history entries while migrating historical schema-v1 bytes only in memory for validation. Direct Redo now validates every Generation ProjectReference restored by its `project.json` snapshot against durable Generation authority and exact current bytes before writing any Redo operation/canonical JSON.

`4774dc60275888f621875bc34f1983ed0588e6a8` replaces archive path-only redo protection with full historical source/artifact ProjectReferences derived from those validated Redo snapshots. Live and redo-only Generation references share the same Job/Attempt/Take/provenance/size/SHA authority before ZIP capture. Conflicting reference ID/path authority fails closed, while non-Generation managed media remains protected by validated redo reachability.

`3eb78fc25a6fd65ce55cac837e4c1816c4eb67d9` applies the same full-reference boundary to startup recovery. Redo-only Generation references are reconstructed and byte/provenance validated before arbitrary-path publication recovery or orphan quarantine can mutate canonical state. Corrupt redo-only Generation bytes therefore cause project recovery to fail closed rather than being preserved as future Redo payload.

Material/test head `3eb78fc25a6fd65ce55cac837e4c1816c4eb67d9` passed CI #4445 (`33630725870`) **5/5 SUCCESS**: development-context, both full Ubuntu/Windows unit suites including the new real Generation/Undo/corruption regressions, and both app-baseline API/real-media/frontend/browser Product Truth jobs all succeeded.

Documentation-only commits `5ac7f34aa1f4da58be7f38b3ec1bd0b20a444b44` and `13b83c84c63071b40e1521202b81168bc547a39e` synchronize `docs/PROJECT_STORE.md` and `docs/PROJECT_ARCHIVES.md` with the repaired contract: Redo reachability is not pathname authority; redo-only Generation bytes remain governed by their full historical ProjectReference plus durable Job/Attempt/provenance and exact size/SHA, including direct Redo.

All earlier Stage-19 repairs remain in force: schema-v1/v2 compatibility and exact historical identity, prepared-UOW recovery before archive sampling, archive locking/snapshot authority, staged/fenced publication, Generation Job/artifact/Take recovery and live digest checks, explicit Take Undo preservation, source/WebVTT/legacy-art/prepared-audio recovery, redo-owned media preservation, publication-marker reference identity, and immediate-next-action Product Truth behavior.

## Next required action

1. synchronize this Draft head and PR body around the accepted ninth repair;
2. run authoritative final Draft exact-head CI 5/5 after body synchronization;
3. reply to and resolve P1 thread `PRRT_kwDOT0Lyms6edmby` only after that evidence is green;
4. recheck all live inline threads for new findings;
5. refreeze context-only from `draft` to `review`, with no further runtime/test/schema/product changes;
6. mark PR #89 Ready without changing the frozen HEAD, require authoritative post-Ready exact-head CI 5/5, and run another completely fresh ordinary-ChatGPT semantic review under BASE `code-review` v1.0;
7. merge only on `CURRENT PASS` with zero findings and clean final live identity.

After merge, D-038 lifecycle closure to `idle` remains mandatory before starting the declared handoff.

## Out of scope

Recipe endpoint retirement, execution-plan retirement, Product Orchestrator redesign/retirement, Stage8 retirement, provider-selection redesign, Production Direction authority changes, Timeline identity redesign and later D-070 compression work remain separate slices.
